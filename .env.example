# Exemplo de variáveis locais (NÃO usar valores reais neste arquivo).
# No Apps Script, configure via Script Properties / menu da planilha — não via .env.
#
# Copie para .env se for usar em scripts locais no futuro.
# O arquivo .env está no .gitignore e não sobe para o GitHub.

APP_TOKEN=seu_app_token_aqui
ACCESS_TOKEN=seu_access_token_aqui
